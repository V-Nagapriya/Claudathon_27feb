{
  "PostToolUse": [
    {
      "matcher": "Edit|Write",
      "hooks": [
        {
          "type": "command",
          "command": "cd client && npx eslint src --fix --quiet 2>&1 | tail -5"
        }
      ]
    }
  ]
}
